git clone 
git checkout -b toanother branch
git checkout master (changed branch)
git status 
git diff to see diffrence
git add + filename or . for all files (adds modified file)
git commit -m 'write your msg' (do when you are sure)
git pull (to pull updated code)
git remote -v (get link of repo)
git push origin branch_name(push code to main link)
git init to inintials a new project in existing folder 

you will have to create new repo from git website than follow CLI commands mentioned their 
like git init etc